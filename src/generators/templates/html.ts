const html_template = `
    <!DOCTYPE html>
    <style>
        * {
            box-sizing: border-box;
        }

        :root {
            margin: 0;
            padding: 0;
        }

        .root {
            display: flex;
            width: 100%;
            height: 100%;
            flex-direction: column;
            align-items: center;
            justify-content: center;
        }
    </style>
`;

export default html_template;